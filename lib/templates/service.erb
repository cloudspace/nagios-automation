define service {
  use                     default-service
  host_name               <%= node_name %>
  service_description     <%= service_name %>
  check_command           <%= service_command %>
  contact_groups          cloudspace
}
